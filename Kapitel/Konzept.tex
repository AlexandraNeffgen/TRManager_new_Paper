In diesem Kapitel wird der konzeptionale Aufbau des Projektes beschrieben.
Dabei werden neben den technischen Konzepten auch die abstrakten Konzepte des
Trainingsraums erläutert. 

\subsection{Trainingsraum}
Das Konzept des Trainingsraumes beruht im wesentlichen auf den folgenden drei
Regeln:
\begin{itemize}
\item Jeder Schüler/jede Schülerin hat das Recht ungestört zu lernen.
\item Jeder Lehrer/jede Lehrerin hat das Recht ungestört zu unterrichten
\item Jeder muss die Rechte der Anderen respektieren 
\end{itemize}
Diesen drei Regeln, oder auch drei Rechte genannt, stimmt jeder Schüler und
Lehrer zu. Sollte ein Schüler wiederholt den Unterricht stören, wird dieser
ausdrücklich ermahnt und vor die Wahl gestellt entweder die Regeln einzuhalten
und das Recht der anderen zu beachten oder aber in den Trainingsraum zu gehen.
Dadurch kann die Zeit, welche zu Aufarbeitung der Störung benötigt wird, aus dem
Unterricht rausgeholt werden und die interessierten Schüler können effektiver
arbeiten. 
Den störenden Schülern wird es so auch ermöglicht in einer entspannter
Atmosphäre ihr Verhalten und die Folgen für sich selbst und
andere zu reflektieren. Im Trainingsraum können sie eigenständig an Ideen zu
Verbesserung ihres Verhalten arbeiten und einen konkreten Plan ausarbeiten um
an der Verbesserung zu arbeiten. 
Zur Unterstützung ist im Trainingsraum während der Unterrichtszeiten stets ein
Pädagoge anwesend, welcher vorwurfsfrei die Schüler bei ihren Ausbarbeitungen
unterstützt und Hilfestellungen bietet.
\cleardoublepage

\subsection{User-Stories}
User-Stories Grundfunktionen
\begin{itemize}
  \item Import und Export der Daten / Datensicherung
  \begin{itemize}
    \item Zu Beginn jedes Schuljahres werden die Schülerdaten aus der externen
    SchILD- Datenbank (Verwaltungsnetzwerk) bezogen und im CSV-Format in das
    Programm (Schülernetzwerk) geladen.
	\item Vorher sollen alle Daten, die das Programm im letzten Schuljahr erfasst
			hat, zunächst exportiert und dann im Programm selbst gelöscht werden.
	\item Der Datentransfer (Import/Export) erfolgt über die USB-Schnittstelle.
	\item Ein Export der vom Programm erfassten Daten soll zu jeder Zeit möglich
			sein. Dabei sollen bei aktiven Filtern / Sortierungen (s. o.) auch nur die
			ausgewählten Daten exportiert werden können.
	\item Zusätzlich soll eine Datensicherungsfunktion implementiert werden, die es
			ermöglicht, alle Daten direkt mit Datumsangabe auf einen externen Datenträger
			(USB) zu speichern.
	\item Diese Funktionen sollen ausschließlich dem Administrator zur Verfügung
			stehen.
  \end{itemize}
  \item Login Daten
  \begin{itemize}
    \item Jeder Lehrer soll sich im Programm mit seinem Kürzel (ohne Passwort)
    anmelden können, um die Informationen des Laufzettels in die grafische
    Oberfläche eintragen zu können.
  \end{itemize}
  \item Besuch eines Schülers eintragen
  \begin{itemize}
    \item Sobald ein Schüler den Trainingsraum betritt, wird zunächst zwischen
    neuen Vorfällen und Rückkehrer unterschieden. Dies geschieht unabhängig vom
    Programm durch den betreuenden Lehrer.
    \item Für Rückkehrer siehe Rückkehrer
    \item Bei einem neuen Vorfall wird ein neuer Incident über ein Menü
    eingegeben. Dabei werden folgende Daten gespeichert:
    \begin{itemize}
      \item Name, Klasse des Schülers, schickender Lehrer und eine Bemerkung zu
      dem Vorfall eingetragen.
      \item Bei der Bemerkung soll zusätzlich zu einem Freitextfeld auch eine
      Auswahlliste mit Standardvorgehen bereitgestellt werden.
    \end{itemize}
    \item Ist der Vorfall eingetragen wird eine fortlaufende
    Überweisungsscheinnummer angezeigt, welche vom betreuenden TR-Lehrer auf
    den Überweisungsschein eingetragen wird. Diese wird im Incident ebenfalls
    gespeichert, aber nicht durch den User ausgewählt.
  \end{itemize}
  \item Vervollständigung
  \begin{itemize}
    \item Bei Eingabe der Namen sollen Vorschläge zur Vervollständigung
    angezeigt werden. Bei keinem Vorschlag soll die Neuanlage eines Schülers
    erfolgen können.
	\item Zur Erleichterung der korrekten Vervollständigung soll zuerst die
	Klassenbezeichnung des Schülers eingegeben werden können, um somit die
	Vervollständigungsvorschläge zu minimieren und den korrekten Schüler
	vorgeschlagen zu bekommen.
  \end{itemize}
  \item Behandlungen bei drei bzw. fünf Besuchen
  \begin{itemize}
    \item Nach Hinzufügen eines Trainingsraumbesuches soll ersichtlich werden,
    wie oft der Schüler in dem jeweiligen Schuljahr bereits im Trainingsraum
    war.
	\item Beim dritten bzw. fünften Besuch soll als Popup ersichtlich werden, dass
	ein Elterngespräch bzw. eine Konferenz notwendig ist.
  \end{itemize}
  \item Behandlung Rückkehrer TR aufgrund von Ablehnung Rückkehrplan
  \begin{itemize}
    \item Kommt ein Schüler ohne neuen Überweisungsschein zurück in den TR, muss
    dies vermerkt werden. Diese Rückkehr ist KEIN neuer TR-Besuch.
    (Rückkehrer-Button)
	\item Gleiche Scheinnummer, neue Incidentnummer
  \end{itemize}
  \item Administrationsrechte
  \begin{itemize}
    \item Es soll ein Administrator-Account mit Passwort angelegt werden, der
    als alleinige Instanz die Möglichkeit haben soll, zeitlich unabhängiges
    Hinzufügen und Verändern bereits eingegebener Daten durchzuführen.
  \end{itemize}
  \item Schülerdatenabruf / Information über Besuche im Trainingsraum (TR)
  \begin{itemize}
    \item Es sollen Schülerdaten (Vor-, Nachname, Id, Klasse und Klassenlehrer),
    sowie Informationen über Besuche im Trainingsraum (Datum, Uhrzeit, Lehrer,
    Anzahl der Besuche) abgerufen und in einer grafischen Oberfläche auf dem
    Bildschirm ausgegeben werden.
	\item Alle Daten (inkl. Lehrer, Klassen) sollen gefiltert und sortiert werden
	können.
	\item Nur zugänglich für den Admin
  \end{itemize}
  \item Zeitzähler
  \begin{itemize}
    \item Es soll ein Zeitzähler realisiert werden, der die Aufenthaltszeit
    jedes Schülers, beginnend bei 0 Minuten hochzählt und vor Erreichen von 15
    Minuten mit einer roten Markierung hinterlegt ist. Bei Erreichen von 15
    Minuten soll die Hinterlegung von rot auf grün umspringen und ein Button
    realisiert werden, mit dem die betreuende Lehrkraft den Schüler (abhängig
    von seinem Rückkehrplan) aus dem Trainingsraum entlassen und aus der Liste
    entfernen kann.
	\item Der Zähler soll solange weiterlaufen, bis der Button gedrückt wurde und
	der Schüler den Raum verlässt.
	\item Kehrt der Schüler aufgrund einer Rückkehrplan-Ablehnung zurück, beginnen
	neue 15 Minuten, was über einen Rückkehrer-Button realisiert werden soll.
	\item Wenn das Programm abstürzt, sollen vorhandene Daten von aktuellen
	Trainingsraumbesuchern nicht verloren gehen. Wird das Programm nach einem
	Absturz wieder gestartet, sollen diese Daten aktualisiert vorhanden sein.
  \end{itemize}
  \item Druckfertige Nachricht
  \begin{itemize}
    \item Ist ein Elterngespräch oder eine Konferenz notwendig, soll eine
    druckfertige Nachricht mit unveränderbarem Template erstellt werden können.
	\item Das Template für diese Nachricht soll abhängig von der Schule gestaltet
	werden können
  \end{itemize}
  \item Neuanlage Lehrer mit hinterlegter Mailadresse
  \begin{itemize}
    \item Neue Lehrer sollen nur vom Administrator angelegt werden können, wobei
    hier ebenfalls eine Emailadresse hinterlegt werden soll.
  \end{itemize}
  \item Mailfunktion
  \begin{itemize}
    \item Es soll eine Emailfunktion vorbereitet werden, die eine Info an die
    beteiligten Lehrer versendet, sobald ein Schüler den TR zum dritten, bzw.
    fünften Mal betritt.
  \end{itemize}
  \item Statistiken anhand der Exportdaten
  \begin{itemize}
    \item Anhand der exportierten Daten sollen lokal Statistiken erstellt werden
    können (Filter- und Sortieroptionen). 
  \end{itemize} 
\end{itemize}
User Stories Unterstützungsfunktionen
\begin{itemize}
  \item Externer Zugriff
  \begin{itemize}
    \item Für die Administration soll ein externer Zugriff auf die Daten über
    eine zusätzliche Software Komponente möglich sein.
	\item Diese Software ist durch zusätzliche Sicherheitsmechanismen zu schützen.
  \end{itemize} 
\end{itemize}

\subsection{Design}

Die Applikation besteht im Grunde aus drei Teilen: Dem Frontend, dem Backend und
der Datenbank. Die Datenbank speichert die Daten, die erfasst werden und
präsentiert diese dem Backend. Das Backend verarbeitet die Daten, die es vom
Frontend und Datenbank bekommt, um es an beide weiterzureichen. Das Frontend
interpretiert die Daten, die es vom Backend geliefert bekommt und zeigt diese
dem Nutzer an und nimmt Eingaben vom Nutzer entgegen und schickt diese an das
Backend.\\

\subsection{Usability}
Zu Usability gibt es keine einheitliche Definition, da Usability in vielen
Bereichen unseres Lebens wichtig ist. So gibt es das Thema Usability bei der
Nutzung von Software, aber auch Verpackungen oder Prozesse müssen "`usable"' sein.
In der DIN EN ISO 9241-11 wird Usability definiert als ein Maß dafür, wie gut
ein Produkt durch bestimmte Benutzer in einem bestimmten Nutzungskontext genutzt
werden kann, um bestimmte Ziele effektiv, effizient und zufriedenstellend (aus
arbeitspsychologischer und -physiologischer Sicht) zu erreichen. Diese
Definition ist in ihrem Kern weitestgehend akzeptiert, jedoch wird von einigen
Autoren dieser Begriff noch erweitert um Elemente wie Spaß oder eine Möglichkeit
der Mitgestaltung des Systems.
Steve Krug verfolgt dabei einen deutlich simpleren Ansatz mit seiner Definition
von Usability: "`Eine Person von durchschnittlicher (oder niederer) Begabung und
Erfahrung kann sich erschließen, wie sie etwas zum Umsetzen ihrer Aufgabe
benutzen muss ohne dabei mehr Schwierigkeiten zu haben als es ihr wert ist"' (4).
Krug unterstützt deshalb auch nicht die Regeln, die beispielsweise besagen, dass
jede Seite nur maximal fünf Klicks entfernt sein darf, sondern solange ein Klick
ohne nachdenken passiert und dem Nutzer anzeigt, dass er auf dem richtigen Weg
ist, können auch mehr als fünf Klicks in Ordnung sein.
Die Definition von Krug soll in dieser Arbeit als die Basis genommen werden. 


\subsection{Backend}

Das Backend dient als Schnittstelle zwischen Datenbank und Frontend und bereitet
die Daten die von Datenbank und Frontend kommen für den jeweils anderen auf.
Umgesetzt wird das Backend mit Spring. Die Zugriffe erfolgen über einen
\acs{REST}ful-Service, der laut REST-Spezifikation die normalen HTTP-Methoden
verwendet (GET, POST etc.). Die Verwendung von \ac{REST} ermöglicht es, die
Clients unabhängig vom Server zu entwerfen, da die Methoden standardisiert und
für jeden Client gleich sind. Auch andere Applikationen können so ohne Probleme
auf diesen Dienst zugreifen, um zum Beispiel die gesammelten Daten anderweitig
zu verwenden. 

\subsubsection{Datenbank}
	Die Datenbank-Struktur wird aus den annotierten Objektklassen generiert.
	Ermöglicht wird dies durch Hibernate, dies wird im Kapitel \ref{Umsetzung des
	Backends} auf seite \pageref{Umsetzung des Backends} erläutert. Als Datenbankserver soll
	MySQL zur Verwendung kommen, da die Datenmengen relativ gering sind und auch Backups Clientseitig realisiert
	werden. Zudem fallen bei MySQL keine Lizenzierungskosten an, da die
	Community-Version als \ac{OSS} zur Verfügung steht.
	 
\subsection{Clients}
	Die Clients haben den Zweck, dem Benutzer die gewünschten Daten
	anzuzeigen.
	Der Zugriff auf diese Daten erfolgt generisch über
	\ac{HTTP}-Requests(\ac{REST}), so dass für jeden Client die gleiche Technik
	verwendet werden kann. Geplant sind mehrere Clients:
	\subsubsection{Native}
		Der native Client verwendet die Bibliotheken von Windows Forms und soll in C\#
		programmiert werden. Dem Benutzer soll auf einem Blick angezeigt werden, wer
		gerade im Trainingsraum ist, wie lange dieser schon darin ist und ob er den
		Raum verlassen darf. Benutzern mit administrativen Rechten soll auch eine
		Verwaltung der Datensätze ermöglicht werden. Diese Funktionen werden für
		"`normale"' Benutzer ausgeblendet.
		
	\subsubsection{Mobile}
		Die mobilen Clients sollen den selben Funktionsumfang haben wie auch der
		native Client. So soll es den Lehrern möglich sein flexibel mit einem Tablett
		die Lokation des Trainingsraumes spontan zu wechseln, ohne dabei an einen fest
		installierten Rechner gebunden zu sein. Beschäftigt man sich allerdings mit
		mobilen Anwendungen, wird man feststellen, dass man dabei zumindest für die
		zwei gängigen Plattformen \gls{Android} und IOS programmieren muss. Die
		unterschiedlichen Plattformen führen zu einem erhöhten Wartungsaufwand. Um
		dies zu verhindern, wird ein Web-Client entwickelt, welcher im nächsten
		Abschnitt vorgestellt wird. 
		
	\subsubsection{Web}
		Der Web-Client soll ähnlich dem nativen Client dem Benutzer die derzeitig
		anwesenden Schüler anzeigen, jedoch wird dieser als Webseite bereitgestellt.
		Die Daten werden über den vom Backend bereitgestellten \ac{REST}ful-Service
		abgerufen und per \ac{AJAX} angezeigt. Die zu verwendenden Frameworks werden
		im Zuge dieser Arbeit für den Einsatz in diesem Umfeld evaluiert und das beste
		ausgewählt. Zur Auswahl stehen \ac{ASP}.NET von Microsoft, \ac{JSF},
		Bootstrap und Zend.
		Der Webclient ermöglicht es die Anwendung sowohl auf einem klassischen
		Desktop-PC zu verwenden, als auch auf mobilen Endgeräten. Durch die Verwendung
		eines Webclients ist es daher nicht nötig eine Applikation für die
		unterschiedlichen mobilen Plattformen zu entwickeln um deren Vorteile für den
		Anwender zu haben.
		
	\subsection{VPN}
		Dadurch, dass wir die Verwaltung der Daten und ihre Darstellung in ein Backend
		und ein Frontend unterteilt haben, ist es auch möglich das Frontend von einem
		anderen Gerät aus aufzurufen. Da die Grundfunktionen der Anwendung nicht
		durch Passwörter geschützt werden sollen muss der Datenschutz an einer Stelle
		realisiert werden. Es soll verhindert werden, dass das Backend aus dem
		\ac{WWW} erreichbar ist. Dazu darf das Backend grundsätzlich nur auf einer
		Maschine in einem geschützten Netz, also etwa hinter einer Firewall,
		installiert werden. Diese Verantwortung liegt allerdings beim Kunden, da
		dieses Projekt nur aus der Applikation als solches besteht. Um dennoch auch
		von außerhalb des geschützten Bereiches mit der Anwendung zu kommunizieren
		dient ein \ac{VPN}. Mit diesem ist es möglich außerhalb eines geschlossenen
		Bereiches sich in diesen einzuwählen und als Mitglied des geschützen Bereiches
		zu agieren. Auch die Anschaffung und Einrichtung eines VPN obliegt dem Kunden
		selbst, da es für die Anwendung als solches nicht nötig ist.
	
